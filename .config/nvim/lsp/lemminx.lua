return {
  cmd = { "lemminx" },
  settings = {
    xml = {
      validate = false,
      format = { enabled = true },
      trace = { server = "off" },
      logs = { client = false },
    },
  },
  filetypes = { "xml", "xsd", "svg", "csproj" },
}
